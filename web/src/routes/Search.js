import React from 'react';
import SearchBar from '../components/SearchBar';

const Search = (props) => {
  return (
    <section>
      <h1>search</h1>
      <SearchBar />
    </section>
  );
}

export default Search;
