const heroFontSize = '52px';
const largeFontSize = '32px';
const mediumFontSize = '24px';
const regularFontSize = '18px';
const smallFontSize = '14px';
const baseFontSize = regularFontSize;

const thinFontFamily = 'mreavesmodot-thin';
const regularFontFamily = 'mreavesmodot-reg';
const heavyFontFamily = 'mreavesmodot-heavy';

const baseFontFamily = regularFontFamily;

const rootTypography = `
  font-feature-settings: "kern" 1;
  text-rendering: optimizeLegibility;
`;

const font = {
  heroFontSize,
  largeFontSize,
  mediumFontSize,
  regularFontSize,
  smallFontSize,
  baseFontSize,
  thinFontFamily,
  regularFontFamily,
  heavyFontFamily,
  baseFontFamily,
  rootTypography,
};

export default font;
